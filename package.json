{
  "name": "gaia",
  "version": "0.1.0",
  "description": "GAIA — crowd-curated music for live events",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"server,web\" --prefix-colors \"magenta.bold,cyan.bold\" --kill-others \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix frontend run dev",
    "build": "npm --prefix server run build && npm --prefix frontend run build",
    "migrate": "npm --prefix server run migrate",
    "reset-db": "npm --prefix server run reset-db",
    "setup": "npm install && npm --prefix server install && npm --prefix frontend install && npm run migrate",
    "install:all": "npm --prefix server install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
